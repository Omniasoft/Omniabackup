# .---------------- Minute (0 - 59)
# |  .------------- Hour (0 - 23)
# |  |  .---------- Day of month (1 - 31)
# |  |  |  .------- Month (1 - 12)
# |  |  |  |  .---- Day of week (0 - 6) where (Sunday=0 or 7)
# |  |  |  |  |
# *  *  *  *  *  module to be executed with arguments
  *  *  *  *  *  S3Postgres -l 31 -p database/postgres backup.devdb.nl server1

# Configuration files
  0  4  *  *  *  s3 -l 7 -p config/apache backup.devdb.nl apache /etc/apache2
  1  4  *  *  *  s3 -l 7 -p config/mysql backup.devdb.nl mysql /etc/mysql
  2  4  *  *  *  s3 -l 7 -p config/postgres backup.devdb.nl postgres /etc/postgresql
  3  4  *  *  *  s3 -l 7 -p config/redmine backup.devdb.nl redmine /var/www/redmine/config
  4  4  *  *  *  s3 -l 1 -p config backup.devdb.nl omniashell /etc/omniashell/cron.conf /etc/omniashell/conf.d
  5  4  *  *  *  s3 -l 7 -p config/jailkit backup.devdb.nl omniashell /etc/jailkit

# Custom scripts
  20 4  *  *  *  s3 -l 1 -f -p scripts/redmine backup.devdb.nl plugins /var/www/redmine/script/update.sh
